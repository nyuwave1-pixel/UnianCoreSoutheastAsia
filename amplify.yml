version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site — no dependencies to install"
    build:
      commands:
        - echo "Static site — no build step required"
  artifacts:
    # 정적 사이트 루트 전체를 배포합니다 (HTML 페이지 + assets).
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
